#include<iostream>
using namespace std;
{
    int largest_element = (int arr[],int n,int tej= INT_MAX,int index){
        if(index==n){
            return tej;
        }
        if(arr[i]>tej){
            tej=arr[i];
            }
            return largest_element(arr,n,index+1,tej);
    }
    int main();
    int arr[]={10,20,30,40,50};
    int n = (size of(arr)/size of(arr[0]));
    int largest_element = find largest_element (arr,n);
    cout<<"find the largest_element into arr"<<endl;
    return 0;
}